#include "main.h"
#include <string.h>

/**
 * _strcmp - compares 2 strings
 * @s1: string 1
 * @s2: string 2
 *
 * Return: 0 Always success
 */

int _strcmp(char *s1, char *s2)
{
	strcmp(s1, s2);
	return (0);
}
