#include <stdio.h>

/**
 * main - prints the name of program
 * @argc: argument count
 * @argv: argument vector
 * Return: void
 */

int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
